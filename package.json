{
  "name": "impds-railway-api",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "postinstall": "python3 -m venv venv && source venv/bin/activate && pip install -r requirements.txt || echo 'Python install skipped'"
  },
  "dependencies": {
    "express": "^4.18.2",
    "axios": "^1.6.2",
    "cheerio": "^1.0.0-rc.12",
    "crypto-js": "^4.1.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
